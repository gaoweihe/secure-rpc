// Communication through legacy TCP sockets functionality 
// before RDMA connection is established. 
#[derive(Debug)]
pub struct RpcGrpc {

}

impl RpcGrpc {
    pub fn get_endpoint(peer_uri: &str) {
        
    }
}